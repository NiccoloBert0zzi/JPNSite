/**
 * Routes that belong to the dark "travel hub" layer (globe landing + its
 * satellite pages). They render LandingNavbar and suppress the light site
 * chrome (Navbar, BackToTop).
 */
export const HUB_ROUTES = ['/', '/viaggi', '/wishlist', '/diario'];
